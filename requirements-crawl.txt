# Crawling/rebuild pipeline (Database/create_db.py, update_db.py).
# Browserless: the site is fully server-rendered, so plain httpx fetching
# works — no crawl4ai/playwright/patchright needed.
trafilatura
langchain-text-splitters
beautifulsoup4
lxml
httpx
markdownify
# Linked-document text extraction (PDF/DOCX). Pure Python, no system libraries —
# the serving VM installs requirements.txt only and never needs these.
pypdf
python-docx
